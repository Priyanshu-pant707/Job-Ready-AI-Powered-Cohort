# Arrays :

- an array is a special variable that can hold multiple values in a single variable .
   -  Arryas are zero indexed
   -  can store mixed data types
   - array are objects internally


```
let arr = [10, 20, 30];
let arr2 = new Array(1, 2, 3);
```

